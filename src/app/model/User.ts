export class User {
  id: number;
  email: string;
  firstName: string;
  lastName: string;
  accessToken: string;
  refreshToken: string;
  // todo to check what suits better here: class or interface
}
